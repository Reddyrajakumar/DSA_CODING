import java.util.Arrays;

public class Society_general_code {
    public static void main(String[] args) {
        Object a[]=new String[2];   //object is parent class of all objects so it all all data types
        a[0]="raja";
        a[1]=100;
        System.out.println(Arrays.toString(a));          ///what is output for this
        //java throw ArrayStoreException
    }
}
